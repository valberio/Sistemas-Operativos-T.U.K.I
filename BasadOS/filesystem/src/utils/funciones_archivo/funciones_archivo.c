#include "funciones_archivos.h"






/*
void leer_archivo(char* nombre_archivo,int direccion_logica, int cantidad_de_bytes){
    
    t_fcb* fcb = buscar_fcb(nombre_archivo*);
    int bloque_base = floor(direccion_logica / tamano_bloque);
    
    
        // busco el bloque base 
        // leo desde la direccion logica hasta el fin del bloque 
        // si cantidad de bytes > tamaño del bloque, busco el bloque siguiente
    
    
}

t_fcb* buscar_fcb(char* nombre_archivo){
    bool matchear_nombre(void* elemento){
        t_fcb* un_fcb = elemento;

        return un_fcb->name == nombre_archivo;
    }

    t_fcb* fcb_encontrado  = list_find(fcb_list, matchear_nombre);

    return fcb_encontrado;
}*/